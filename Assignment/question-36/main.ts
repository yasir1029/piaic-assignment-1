function make_shirt(size: string, message: string): void {
    console.log(`The shirt is size ${size} and it says: "${message}".`);
}

make_shirt("Large", "too Bulky!");
make_shirt("medium", "perfect Size!");
make_shirt("small", "Its too small!");